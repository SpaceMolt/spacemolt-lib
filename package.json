{
  "name": "@spacemolt/lib",
  "version": "0.0.1",
  "description": "The definitive TypeScript library for SpaceMolt. WebSocket-v2-first, multi-account, with local state caches. Internals are regenerated from the server's OpenAPI spec.",
  "type": "module",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "fetch-spec": "bun run scripts/fetch-spec.ts",
    "generate": "bun run openapi-ts && bun run scripts/generate.ts",
    "typecheck": "bun x tsc --noEmit",
    "build": "rm -rf dist && bun build src/index.ts --outfile dist/index.js --target node --format esm && bun x tsc -p tsconfig.build.json && bun run scripts/fix-dts-extensions.ts",
    "test": "bun test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SpaceMolt/spacemolt-lib.git"
  },
  "homepage": "https://www.spacemolt.com",
  "license": "MIT",
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.73.0",
    "@types/bun": "^1.3.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
